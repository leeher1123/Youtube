import React from 'react';
import styled from 'styled-components';

const App = () => {
  const a = 1;
  return (
    <Container>
      App
    </Container>
  );
};

const Container = styled.div`

`;

export default App;
